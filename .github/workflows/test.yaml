# SPDX-License-Identifier: MIT

name: Tests

on:
  schedule:
    # Run each Sun
    - cron: '0 0 * * 0'
  push:
  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3.5.2
  
      - name: Run tests
        run: python src/facere_sensum/test.py
